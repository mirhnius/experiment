#!/bin/sh

#first the producing code 
#get number of run (same as the bash one)
#get the parent directory (same as the bash one)
#in the container --> for that I think it is better to find the last part and build up on that 
# I think it is better to not change the singularity path or maybe use the parent directory in out part
# and untouch the rest

#getteing parent_directory
#get number of run
#create the err and out directories if they do not exist
#the bash job

#anlysis